{
  "name": "selenium-trial-1",
  "version": "1.0.0",
  "description": "This is a trial project to practice Selenium",
  "main": "index.js",
  "scripts": {
    "test": "mocha --no-timeouts",
    "para": "mocha --no-timeouts --parallel",
    "report": "mocha --no-timeouts --reporter mochawesome",
    "parareport": "mocha --no-timeouts --parallel --reporter mochawesome --require mochawesome/register"
  },
  "keywords": [],
  "author": "",
  "license": "ISC",
  "dependencies": {
    "chai": "^4.3.7",
    "mocha": "^10.2.0",
    "mochawesome": "^7.1.3",
    "selenium-webdriver": "^4.9.1"
  }
}
